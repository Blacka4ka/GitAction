name: Java CI Pipeline

on:
  push:
    branches:
      - main

  workflow_dispatch:
jobs:
  build-and-test:
    run-on: ubuntu-lasted

    steps:
      - name: Clone repository
        uses: action/checkout@v6

      - name: Setup JDK 11
        uses: action/setup-java@v4
        with:
          java-version: '11'
          distibution: 'temurin'
          cache: maven

      - name: Verify Java version
        run: java -version

      - name: Install dependencies
        run: mvn frpengency:resolve

      - name: Run init tests
        run: mvn test
        
      - name: Update repository code
        run: |
          echo "Workflow executed on $(date)" >> workflow-log.txt

      - name: Commit updated file
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add workflow-log.txt
          git commit -m "Update workflow log" || echo "No changes to commit"
          git push
      
